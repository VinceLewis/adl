<!-- qa-kit {"schemaVersion":1,"kind":"acceptance-criterion","id":"AC-0004","requirementId":"REQ-0001","status":"active"} -->

# AC-0004: Unusable names receive the deterministic fallback

An input made only of punctuation has no usable storage characters, so it
falls back to `unnamed` instead of producing an empty identifier. This failure
case keeps downstream storage metadata safe and deterministic.
